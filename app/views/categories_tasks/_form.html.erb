<%= form_for(@categories_task) do |f| %>
  <% if @categories_task.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@categories_task.errors.count, "error") %> prohibited this categories_task from being saved:</h2>

      <ul>
      <% @categories_task.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :category_id %><br />
    <%= f.number_field :category_id %>
  </div>
  <div class="field">
    <%= f.label :task_id %><br />
    <%= f.number_field :task_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
